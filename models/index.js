// import all your models here then export a global object
// it's generally good practice to create a separate file for each model
// Ex: you want to create a Person model
// 1. create a /modesl/Person.js file
// 2. write all the logic
// 3. import it using require ( ie const Person = require("./Person") )
// the end of the file should look something like this:
//
// module.exports = { Person, YourOtherModel, YetAnotherModel }
// notice how we use ES6 destructuring to avoid writing key-value pairs
